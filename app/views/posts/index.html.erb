
<% @posts.each do |post| %>

	<div class="post">
		<p><%= post.title %></p> 
		<p><%= post.content %></p>

		<%= link_to "#{post.user.full_name}", post %>
		<%= link_to "Edit", edit_post_path(post) %>
		<%= link_to "Destroy", post, method: :delete %>
	</div>

<% end %>
